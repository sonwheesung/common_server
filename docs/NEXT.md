# 다음 작업 (2026-09-02~)

**미결 목록**이다. 끝나면 지우거나 PLAN으로 옮긴다 — 남겨두면 썩는다.
설계 배경은 `docs/PLAN.md` Phase 12·13, 세션 간 협업은 `docs/MULTI_SESSION.md`,
스토어 현황은 `C:\project\common\PLAY_CONSOLE_STATUS.md`(콘솔 실측 정본).

---

## ✅ 끝난 것 — 2026-09-02 (설계는 PLAN Phase 13으로 옮김)

- **웜 스타트 하트비트** `POST /v1/heartbeat` + SDK `heartbeat()` — 배포 완료(`f418c0f`)
- **`exp` 클레임 이관** — 만료 기준이 서버 한 곳으로. `SESSION_TTL_DAYS`는 레거시 폴백으로 강등
- **SDK `2026-09-02`** — 네 세션에 스펙 발송 완료(재복사 + `AppState` 리스너)

검증: 프로덕션 가드 `_dv_public 14` · `_dv_auth 27` · `_dv_sdk 41` — 전부 PASS.
성공 경로는 `tools/_e2e_heartbeat.ts` 10/10(로컬·프로덕션 각 1회, 테스트 행은 정리).

---

## 🔴 1. 앱 회신 대기 — 이번엔 **호출부가 늘어난다**

지금까지 SDK 갱신은 전부 재복사만으로 끝났다. 이번엔 앱마다 `AppState` 리스너를 붙여야 하므로
**"복사했다"가 곧 "동작한다"가 아니다.** 확인할 축이 하나 늘었다.

| 앱 | 붙일 자리 | 배송 | 상태 |
|---|---|---|---|
| `my_word` | `BootstrapContext` | **OTA**(런타임 1.3.3) | ⏳ R8 E2E를 이 착지 뒤로 미뤄 둠 |
| `linkmemo` | `boot-gate.tsx` | **OTA**(런타임 1.0.0) | ⏳ |
| `idearepository` | `boot-gate.tsx` | **vc11에 내장**(AAB 재빌드 완료) | ✅ 반영 완료(`d4649f8`·`ec16a42`). 🎉 **프로덕션 액세스 권한 부여됨**(9/2 콘솔 실측) — 다만 트랙은 아직 **비활성**이고 vc11 업로드가 다음 관문(rev.6 게시 선행) |
| `jogak` | `app/_layout.tsx` | **vc17에 내장**(expo-updates 없음) | ✅ 반영 완료(`0a7b7ce`) — 라이브는 vc17부터. vc16 검토 끝난 뒤 |

🔴 **처리방침 문안이 같이 바뀐다 — 내가 스펙에 안 적었다.**
저장 항목은 그대로인데 **전송 시점**이 *"실행 시 1회"* → *"실행 시 + 복귀 시(5분 간격)"* 로 넓어진다.
수집 항목이 안 늘었다고 문안이 그대로여도 되는 게 아니다 — 처리방침이 **빈도를 서술하고 있으면**
그 문장이 사실과 어긋난다. idea_repository 세션이 스스로 찾아 rev.6(미게시)에 편입했다.
→ 나머지 세 앱도 자기 문안에 "1회"·"실행 시"류 서술이 있는지 확인해야 한다(2026-09-02 전달함).
⚠ **게시된 문안을 고치는 앱은 시행일·고지 기간을 봐야 한다** — 미게시 rev 에 얹으면 불일치 창이 없다.

~~⚠ **조각은 결정이 하나 남았다**~~ → **끝났다**(2026-09-02): `signedIn ? commonServer : deviceServer`.
기기 고정을 안 쓴 이유는 `CLAUDE.md` 조각 절에 있다 — 내 권고가 틀렸고 그 이유가 우리 설계의 성질이다.

⚠ **원래 있던 결정 문단(아래)은 참고용으로 남긴다** — 기기 subject / 구글 subject 2칸 분리 중이라
"어느 세션으로 하트비트를 찍나"를 정해야 한다. 권고는 **기기 세션**(로그인 여부와 무관하게 항상 있고,
DAU의 정의가 "앱을 켠 사람"이다). 정해지면 콘솔의 활성 정의 문구를 맞춘다.

**확인 방법**: 붙은 뒤 그 앱의 DAU가 **콜드 스타트 수보다 커지면** 리스너가 실제로 도는 것이다.
안 커지면 복사만 되고 호출부가 안 붙은 것이다 — 서버에서는 이 둘이 구분되지 않으므로 물어봐야 한다.

---

## 2. 다크 테마 대비 — 셋이 AA 미달

버튼은 어제 고쳤다(3.70 → 5.24). 같은 방식으로 훑으니 셋이 더 걸린다. **계산해서 나온 처방**:

| 대상 | 현재 | 처방 | 결과 |
|---|---|---|---|
| **보조 텍스트** `--color-fg-muted` | 3.26 | L72 → **L78** | 4.77 ✅ (muted 배지 4.70도 같이 해결) |
| accent 배지 | 3.60 | `--color-accent` L70 → **L78** | 4.92 ✅ ⚠ 차트도 밝아짐 — 눈으로 확인 필요 |
| ok 배지 | 4.31 | `--color-ok` L72 → **L76** | 5.60 ✅ |

⚠ `accent-soft` **배경을 어둡게 하는 방식은 안 통한다**(3.60 → 3.69). 글자 쪽을 밝혀야 한다.

**보조 텍스트가 우선순위 1번**이다 — 캡션·표 메타·모든 `sub`가 그 색이고 11~12px이다.

---

## 3. 관측: 저절로 켜지는 것들

손댈 필요 없이 시간이 해결한다. **안 켜지면 그때 의심한다.**

- 시간대별 활성 — `hourCoverageDays ≥ 3`이면 켜진다(2026-09-01부터 누적 → 9/3 무렵)
- 요일 평균 — 수집 14일차에 켜진다
- 잔디 — 날짜가 쌓일수록 채워진다. 2026-09-01 이전은 **점선(수집 전)** 으로 남는 게 정상이다

🔴 ⚠ **웜 스타트 계측이 붙기 전과 후는 같은 축이 아니다.** 붙은 날 그 앱의 DAU가 뛰면
그건 사용자가 는 게 아니라 **계측이 는 것**이다. 화면이 그걸 말하지 않으면 다음 사람이
"9월 초에 성장했다"로 읽는다 — Phase 12가 막아둔 거짓말 셋과 같은 종류다.
→ 앱별 **계측 개시일**을 콘솔이 알고 있어야 한다(지금은 `coverageDays`가 서버 전체 축이다).

---

## 4. 리텐션 D1/D3/D7 (미착수)

`subject_active_day`가 있으므로 **근사가 아니라 진짜 코호트**를 만들 수 있다(배구는 `lastSeenAt` 근사).
다만 D7은 7일치가 쌓여야 말할 수 있고, **위 3번이 먼저다** — 계측이 바뀐 경계를 걸치는 코호트는
리텐션이 좋아진 것처럼 보인다(다음 날 다시 잡힐 확률 자체가 올라가므로).

---

## 5. 문서 규율 — 이번에 새로 생긴 것

- `.claude/skills/reload-docs/SKILL.md` §1.8 — 컴팩트 후 Play Console 현황을 크롬으로 갱신(조건부, 읽기 전용)
- `C:\project\common\PLAY_CONSOLE_STATUS.md` — 스토어 실황 정본.
  ⚠ **§2-1(콘솔)과 §2-4(OTA)를 함께** 봐야 한다 — 콘솔만 보면 "사용자가 실제로 도는 코드"를 모른다.
