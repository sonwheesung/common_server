# 다음 작업 (2026-09-02~)

2026-09-01 하루치 작업의 **미결 목록**이다. 끝나면 지우거나 PLAN으로 옮긴다 — 남겨두면 썩는다.
설계 배경은 `docs/PLAN.md` Phase 12, 세션 간 협업은 `docs/MULTI_SESSION.md`.

---

## 🔴 1. 웜 스타트에 하트비트가 없다 — 지표 신뢰도가 여기 걸려 있다

**사용자가 발견**: "앱에서 홈 누르고 다시 들어가는 건 접속 정보로 안 나오는 것 같다." 맞다.

하트비트는 `fetchBootstrap()`에 얹혀 있고, 그건 **JS 프로세스당 1회**만 불린다:

| 앱 | 트리거 | 재실행 조건 |
|---|---|---|
| linkmemo · idearepository | `boot-gate.tsx` → `useBootStore.fetchOnce()` | `fetched` 플래그 — 프로세스당 1회 |
| my_word | `BootstrapContext` `useEffect(…, [])` | 프로바이더 마운트 1회 |
| jogak | `app/_layout.tsx` → `useNoticeStore.load()` | `loaded` 플래그 — 프로세스당 1회 |

RN에서 홈 버튼은 프로세스를 죽이지 않는다. 포그라운드 복귀 시 루트가 다시 마운트되지 않으므로
**웜 스타트에는 하트비트가 한 번도 안 나간다.**

**결과 (셋 다 실제 왜곡)**

1. **DAU 과소집계** — 앱을 한 번 켠 뒤 안 죽이면 **다음 날부터는 아예 안 잡힌다.**
   앱을 열심히 쓰는 사용자일수록 덜 잡히는, 방향이 거꾸로인 오차다.
2. **최근 접속자(30분)** — `lastSeenAt`이 콜드 스타트 시각에 고정돼 거의 항상 0에 가깝다.
3. **시간대별 활성** — "몇 시에 쓰고 있었나"가 아니라 **"몇 시에 앱을 처음 켰나"** 의 분포다.

**할 일**

- 서버: `POST /v1/heartbeat` 신설. 세션 토큰만 받고 `recordActive()`만 한다.
  `bootstrap`을 다시 부르는 방식은 쓰지 않는다 — 페이로드가 크고 공지 읽음 상태를 흔든다.
  응답은 비워도 되고, 갱신 토큰(`session.token`)은 같이 실어도 좋다.
- SDK: `AppState` 전이 `→ 'active'`에서 호출. **앱 쪽 쿨다운 5분**(그 안이면 스킵).
  서버는 이미 멱등이라 중복은 무해하지만, 네트워크를 아끼는 건 앱의 몫이다.
- 🔴 **실패는 완전히 조용하다 — 반환값도 주지 않는다.** `Result` 를 돌려주면 호출부가
  그걸 화면에 표시하고 싶어진다. 포그라운드 복귀는 **부팅보다 훨씬 자주** 일어나므로,
  지하철에서 앱을 켜는 매번 오류가 뜼게 된다. **관측이 사용자 눈에 보이면 설계가 틀린 것이다.**
  (my_word 지시 사항: *"인터넷이 안 되면 오류 표시가 나면 안 된다"* — 부팅 경로는 이미 그렇게 돼 있다)
  → 시그니처는 `heartbeat(): Promise<void>`. **실패할 수 있다는 사실 자체를 안 드러낸다.**

  🔴 **계약: `heartbeat()`는 절대 reject 하지 않는다.**
  네트워크 오류·타임아웃·미설정·세션 없음·쿨다운 미충족을 전부 내부에서 삼키고 조용히 resolve 한다.
  호출부는 `.catch()` 없이 불러도 된다.
  ⚠ **타입이 `void`인 것만으로는 부족하다** — 거부되는 프라미스는 그대로 샐다:
  ```ts
  AppState.addEventListener('change', (st) => {
    if (st === 'active') server.heartbeat();   // .catch() 없음 — 리스너는 동기라 잡을 곳이 없다
  });
  ```
  여기서 throw 하면 **unhandled rejection** 이다 — 개발 빌드는 빨간 박스, `ErrorBoundary`/`ErrorUtils`를
  둔 앱은 **그게 그대로 오류 UI 로 간다**(my_word 에 `ErrorBoundary` 있음).
  🚫 호출부에 `.catch(() => {})` 를 요구하지 않는다 — **잊는 사람이 반드시 나오고**,
  잊었을 때의 증상이 정확히 이 API 가 막으려던 그것이다. 방어를 밖으로 미루면 `void` 를 고른 이유가 사라진다.
  (지적: my-word 세션 2026-09-02) → 가드로 강제한다(`_dv_sdk.ts`: 서버가 죽어도 reject 하지 않는다).
- 덤: 밤 11시에 켜서 새벽 1시까지 쓰는 사용자의 **자정 넘김**도 같이 해결된다.

⚠ SDK가 또 오르므로 **`exp`(아래 2번)와 같은 판에 묶어야 한다.** 따로 내면 앱이 두 번 돈다.

---

## 🔴 2. `exp` 클레임 이관 — **막고 있던 세션이 없다**(2026-09-02 정정)

~~**결정만 남았다.** linkmemo 세션이 빌드 전 확인을 규칙으로 박아두고(OTA_UPDATE §7) 기다리고 있다.~~

**정정 — 전제가 틀렸다.** Play Console 실측(2026-09-02 11:07 KST) 결과 **linkmemo vc11은 어제
20:33에 이미 프로덕션에 게시됐다**(1.0.7 · 177개국). SDK `.2`와 `expo-updates`를 싣고 나갔다.
즉 **아무도 이 결정을 기다리고 있지 않았고**, 나는 반나절 지난 사실 위에 하루치 계획을 세웠다.

바뀌는 것 둘:
- **급하지 않다.** "남을 막고 있다"가 1·2번을 묶어야 할 이유였는데 그게 사라졌다. 다만 **묶는 게
  여전히 낫다** — 이유가 "앱을 두 번 돌리지 않기"로 바뀐 것뿐이다.
- 🟢 **linkmemo는 이제 OTA로 받는다.** vc11에 `expo-updates`가 들어갔으므로 `.3`은 **AAB 없이**
  나갈 수 있다(runtimeVersion `1.0.0` 유지가 조건). vc12를 굽지 않아도 된다.

상태는 `C:\project\common\PLAY_CONSOLE_STATUS.md`가 정본이다.

지금은 만료 기준이 두 곳에 복제돼 있다:

```
서버  lib/auth/session.ts   TOKEN_TTL_MS      = 180일
SDK   client/index.ts       SESSION_TTL_DAYS  = 180      ← 사본
```

값이 같아 지금은 무해하지만, 나중에 누가 서버만 바꾸면 **조용히 갈라진다.** 조각 세션이 짚었고,
"어긋나면 앱이 더 보수적인 쪽으로"라는 규율은 **사람이 지켜야 유지된다.**

**할 일**

```
signSession    → { sid, app, iat, exp: Date.now() + TOKEN_TTL_MS }
verifySession  → exp 있으면 exp 기준, 없으면 iat + TTL 폴백(기발급분 호환)
SDK tokenAlive → exp 우선, SESSION_TTL_DAYS는 레거시 폴백으로 강등
SDK_VERSION    → 2026-09-01.3  (또는 1번과 묶어 2026-09-02)
```

그러면 서버가 유일한 진실이 되고, TTL을 바꿔도 다음 갱신(최대 30일)에 자동 전파된다 — **앱 재배포 불필요.**

**TTL 숫자는 180일 그대로 둔다.** 올릴 이유였던 "이미 나간 구제 대상"이 실측으로 사라졌다
(myword는 OTA로 덮고, linkmemo vc10은 설치 0회, idearepository는 프로덕션 미출시).
`exp`가 들어가면 나중에 언제든 서버 한 줄로 바꿀 수 있으므로 지금 정할 필요가 없다.

---

## 2. 다크 테마 대비 — 셋이 AA 미달

버튼은 오늘 고쳤다(3.70 → 5.24). 같은 방식으로 훑으니 셋이 더 걸린다. **계산해서 나온 처방**:

| 대상 | 현재 | 처방 | 결과 |
|---|---|---|---|
| **보조 텍스트** `--color-fg-muted` | 3.26 | L72 → **L78** | 4.77 ✅ (muted 배지 4.70도 같이 해결) |
| accent 배지 | 3.60 | `--color-accent` L70 → **L78** | 4.92 ✅ ⚠ 차트도 밝아짐 — 눈으로 확인 필요 |
| ok 배지 | 4.31 | `--color-ok` L72 → **L76** | 5.60 ✅ |

⚠ `accent-soft` **배경을 어둡게 하는 방식은 안 통한다**(3.60 → 3.69). 글자 쪽을 밝혀야 한다.

**보조 텍스트가 우선순위 1번**이다 — 캡션·표 메타·모든 `sub`가 그 색이고 11~12px이다.

---

## 3. 앱별 남은 것

| 앱 | 남은 것 |
|---|---|
| `idea_repository` | 비공개 테스트 vc10(1.0.9) 제공 중. SDK `.2` 재복사 커밋됨(0930a4c). ~~`expo-updates` 미설치~~ → **저장소엔 편입 완료**(커밋 `59a4638`), 다만 **라이브 vc10엔 안 실려 있다** — 네이티브라 OTA로 못 켠다. vc11(1.0.10) 빌드·검증 끝, 처리방침 rev.6 게시 대기로 **업로드 보류** |
| `linkmemo` | ✅ **vc11(1.0.7) 프로덕션 게시 완료**(9/1 20:33 · 177개국 · SDK `.2` · expo-updates 탑재). `.3`은 **OTA로** 나갈 수 있다 |
| `my_word` | ✅ **19(1.3.3) 프로덕션 게시**(9/1 18:36 · 145개국) + ✅ **OTA 발행 완료**(9/1 ≈21:15, 런타임 1.3.3 · 채널 production). ~~OTA가 안 나갔다~~ — 내 오판이었다(아래 ⚠). **남은 것 없음** |
| `jogak` | SDK `.2` 완료. 비공개 테스트 vc15(0.2.2) 제공 중 · **vc16(0.2.3) 검토 중**(9/2 11:02). 2칸 분리 + 연령 게이트 + 처리방침 개정 진행 중 |

> 스토어 상태의 정본은 **`C:\project\common\PLAY_CONSOLE_STATUS.md`** (2026-09-02 신설).
> 위 표는 그 스냅샷이다 — 어긋나면 그쪽이 맞다.

⚠ **정정(2026-09-02) — "게시 직후 OTA로 덦는다"는 순서 의존이 아니다.**
EAS Update 는 **`runtimeVersion` + 채널**로 매칭한다 — 시각이 아니다. 게시 **전**에 발행해 둔
업데이트도 그 자리에 있다가 빌드가 게시되면 받아간다. 오히려 **먼저 올려 두는 편이 안전**하다
(게시와 발행 사이에 켜 사용자가 옛 번들을 안 받는다). 진짜 순서가 걸리는 건 `runtimeVersion` 이
바뀔 때뿐이다. — my-word 세션 지적, 출처 `PLAY_CONSOLE_STATUS.md §2-4`.

🟢 **그래서 `.3` 배포가 생각보다 싸다.** 웹 스타트 하트비트는 `AppState` 리스너 + SDK 교체라
**JS 만 바뀐다** → my_word·linkmemo 는 **AAB 없이 OTA 로** 받는다.
~~`runtimeVersion` 을 안 올리는 게 조건~~ — 아니다. 그 앱들은 **네이티브 모듈이 바뀔 때만**
`runtimeVersion` 을 올린다(my_word `CLAUDE.md:116`). 앱 버전과 같아 보이는 건 우연이다 —
**버전을 올려도 같은 OTA 를 받는다.** (출처: my-word 세션 2026-09-02 · `PLAY_CONSOLE_STATUS §2-4`)

---

## 4. 관측: 저절로 켜지는 것들

내일 손댈 필요 없이 시간이 해결한다. **안 켜지면 그때 의심한다.**

- 시간대별 활성 — `hourCoverageDays ≥ 3`이면 차트가 켜진다(오늘 배포분부터 누적)
- 요일 평균 — 수집 14일차에 켜진다
- 잔디 — 날짜가 쌓일수록 채워진다. 오늘 이전은 **점선(수집 전)** 으로 남는 게 정상이다

---

## 오늘(2026-09-01) 한 것 — 커밋 순

```
8167c23  활성 지표(DAU/WAU/MAU) + 관리자 콘솔 2차
35dd7ff  콘솔 토큰 30일 슬라이딩
4d2a84a  지표에 출처·정의·고치는 법 (배구 콘솔 전수조사 반영)
060aaa1  세션 토큰 슬라이딩 갱신 ← 만료가 사용자를 DAU에서 조용히 지우던 버그
2aba6ed  조각 subject 2행 문서화
5fa01d6  최근 접속자(30분) — lastSeenAt 해상도 복원
0eb1e73  시간대별 활성(KST 0~23시) — 시각 비트마스크
4d91200  Play 데이터 보안 실측 2건
74b4e71  docs/MULTI_SESSION.md
6ff4d9b  다크 primary 버튼 대비 (AA 미달 3.70 → 5.24)
8ec59b7  사용자 활동 달력(잔디) + 목록 정렬 축
```

DB 변경 2건(둘 다 additive): `subject_active_day` 테이블, `hours` 컬럼.
