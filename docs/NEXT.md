# 다음 작업 (2026-09-03 갱신)

**미결 목록**이다. 끝나면 지우거나 PLAN으로 옮긴다 — 남겨두면 썩는다.
설계 배경은 `docs/PLAN.md` Phase 12·13, 세션 간 협업은 `docs/MULTI_SESSION.md`,
스토어 현황은 `C:\project\common\PLAY_CONSOLE_STATUS.md`(콘솔 실측 정본).

---

## ✅ 끝난 것 — 2026-09-02 (설계는 PLAN Phase 13으로 옮김)

- **웜 스타트 하트비트** `POST /v1/heartbeat` + SDK `heartbeat()` — 배포 완료(`f418c0f`)
- **`exp` 클레임 이관** — 만료 기준이 서버 한 곳으로. `SESSION_TTL_DAYS`는 레거시 폴백으로 강등
- **SDK `2026-09-02`** — 네 세션에 스펙 발송 완료(재복사 + `AppState` 리스너)

검증: 프로덕션 가드 `_dv_public 14` · `_dv_auth 27` · `_dv_sdk 41` — 전부 PASS.
성공 경로는 `tools/_e2e_heartbeat.ts` 10/10(로컬·프로덕션 각 1회, 테스트 행은 정리).

---

## ✅ 1. 앱 회신 — **끝났다. 넷 다 코드가 붙었고 셋은 사용자에게 닿았다**

`SDK 2026-09-02` + `AppState` 리스너 부착 현황. **확인 시각 2026-09-03 10:31~10:52 KST**
(서버 신호 `/api/admin/stats` 4앱 동시 + Play Console 직접 조회 + diary 세션 회신).

| 앱 | 코드 | 스토어 | 서버 웜 신호 | 판정 |
|---|---|---|---|---|
| `myword` | ✅ `08c0370` | 프로덕션 19/1.3.3 (9/1 18:36 · 145국) | ✅ 2026-09-02 | 🟢 도달 |
| `linkmemo` | ✅ `4dea674` | 프로덕션 11/1.0.7 (9/1 20:33 · 177국) | ✅ 2026-09-02 | 🟢 도달 |
| `idearepository` | ✅ `d4649f8` | 프로덕션 11/1.0.10 (**9/2 15:29** · 177국) | ✅ 2026-09-02 | 🟢 도달 + **자연 도달 확정** |
| `jogak` | ✅ `3b57e69`<br>빌드 `a88a732` | 비공개 Alpha **17/0.2.4** (**9/2 15:50**) | ❌ `warmSince: null` | 🟡 **판정 불가**(아래) |

🟢 **`idearepository`가 이 계정의 첫 자연 도달 표본이다.** 출시 당일(9/2) DAU **24** · 그중 신규 주체 **23** ·
9/3 오전 10:31 시점에만 **11**(누적 36 · 지금 접속 중 11). §5의 `DAU ≥ 2` 오라클을 압도적으로 넘는다 —
내부 기기를 몇 대로 잡아도 24는 안 나온다. **문의도 2건 들어왔고 둘 다 처리 완료다.**

🔴 **`jogak`의 웜 0은 "미부착"이 아니다 — 서버는 그 둘을 구분하지 못한다.**
판정 입력이 **웜 신호 유무 하나뿐**이라 아래 둘이 같은 화면이 된다:

```
① 코드가 안 붙었다        → 진짜 미부착
② 그날 아무도 앱을 안 켰다 → 웜 이벤트가 원리적으로 0
```

조각은 ②다. 코드는 **vc17 빌드 커밋 `a88a732`에 실제로 들어 있고**(`app/_layout.tsx:98` → `void beat();`,
`SDK_VERSION = '2026-09-02'` — diary 세션이 트리에서 직접 확인), 그 앱 **9/3 DAU가 0**이다.
→ 판정은 **테스터가 vc17로 올라간 뒤 하루는 보고** 한다. 지금 `warm_uncollected`를 사실로 굳히면 오진이다.
→ ★ 또 **값은 맞고 해석이 사실을 넘는** 형태다. 오늘로 세 번째다(`warmSince` · `DAU≥2` 오라클 · 이번).
   `CLAUDE.md` 연동 앱 표 아래에 규칙으로 박았다.

⚠ **`linkmemo`·`myword`의 9/1 스파이크(9·8)를 성장으로 읽지 마라.** 그날 기기 subject 도입 + 계측 변경이
겹쳤다. 계측이 바뀐 날의 점프는 사용자가 아니라 **계측이 는 것**이다(§3의 경계 규칙).

⚠ 조각 배선 하나가 아직 비어 있다 — `DISCORD_TICKET_WEBHOOK_URL_JOGAK` 미설정이라 **문의가 와도 모른다.**
   env는 **재배포해야 적용**된다. 발급은 사람이 하고 나는 이름과 주입 위치만 정한다.

---

## ✅ 2. 색 대비 — **내가 어제 틀렸다**(2026-09-02 정정, 커밋 아래)

~~다크 테마 셋이 AA 미달: 보조 텍스트 3.26 · accent 배지 3.60 · ok 배지 4.31~~
→ **전부 틀린 숫자였다.** 다시 계산하니 각각 **7.25 · 5.20 · 6.30 으로 원래부터 통과**였다.
그 "처방"대로 고쳤으면 멀쩡한 색을 셋 더 흔들 뻔했다.

🔴 **그리고 어제 실제로 한 수정(커밋 `6ff4d9b`)이 반대였다.**
"L70 파랑 위 근검정이 3.70:1 미달"이라며 근백으로 바꿨는데, 실제 값은:

```
근검정  6.66:1  통과   ← 원래 상태
근백    2.65:1  미달   ← 내가 바꾼 상태
```

밝은 면 위에서는 **어두운 글씨의 대비가 크다.** 착시가 아니라 산수인데 반대로 적었고,
그 숫자가 커밋 메시지·CSS 주석·이 문서 세 곳에 그대로 퍼졌다.
⚠ 사용자가 "파란 버튼에 검은 글씨가 이상하다"고 한 것은 **맞았다** — 관례상 어색한 게 맞다.
틀린 건 내가 거기에 **가짜 접근성 근거를 붙인 것**이다.

**고친 방법**: 글자를 뒤집는 게 아니라 **면을 나눴다**. `--color-accent` 하나가 세 역할
(차트 막대 / 배지 글자 / 버튼 배경)을 겸해 한 값으로 셋을 만족시킬 수 없었다.
글자를 얹는 면만 `--color-accent-strong`(다크 L54)으로 분리 — 흰 글씨 5.04:1.
덤으로 **원래부터 미달이던 라이트 warn 배지**(3.95:1)도 찾아 고쳤다(L58 → L52, 5.07:1).

**재발 방지**: `tools/_dv_contrast.ts` 신설. `globals.css`를 **읽어서** 27개 조합을 계산하고
미달이면 FAIL 한다. 오라클 5개가 계산기 자체를 먼저 검증한다(그중 하나가 "밝은 파랑 위에선
검은 글씨가 흰 글씨보다 대비가 크다" — 어제 틀린 그 명제다).
→ **이제 대비는 주장할 수 없고 계산된다.**

---

## 3. 관측: 저절로 켜지는 것들

손댈 필요 없이 시간이 해결한다. **안 켜지면 그때 의심한다.**

- ~~시간대별 활성 — `hourCoverageDays ≥ 3`이면 켜진다(2026-09-01부터 누적 → 9/3 무렵)~~
  → ✅ **켜졌다.** 2026-09-03 실측 `myword` `hourCoverageDays: 3 · hourChartReady: true`.
  ⚠ **앱마다 값이 다르다** — 같은 날 `idearepository`는 `2 / false`다. 게이트는 서버 전체가 아니라 **그 앱의 수집 경과일**을 본다.
- 요일 평균 — 수집 14일차에 켜진다(2026-09-03 현재 `coverageDays: 3` · `chartReady: false` — 정상)
- 잔디 — 날짜가 쌓일수록 채워진다. 2026-09-01 이전은 **점선(수집 전)** 으로 남는 게 정상이다

🔴 ⚠ **웜 스타트 계측이 붙기 전과 후는 같은 축이 아니다.** 붙은 날 그 앱의 DAU가 뛰면
그건 사용자가 는 게 아니라 **계측이 는 것**이다. 화면이 그걸 말하지 않으면 다음 사람이
"9월 초에 성장했다"로 읽는다 — Phase 12가 막아둔 거짓말 셋과 같은 종류다.
→ 앱별 **계측 개시일**을 콘솔이 알고 있어야 한다(지금은 `coverageDays`가 서버 전체 축이다).

---

## 4. 리텐션 D1/D3/D7 (미착수)

`subject_active_day`가 있으므로 **근사가 아니라 진짜 코호트**를 만들 수 있다(배구는 `lastSeenAt` 근사).
다만 D7은 7일치가 쌓여야 말할 수 있고, **위 3번이 먼저다** — 계측이 바뀐 경계를 걸치는 코호트는
리텐션이 좋아진 것처럼 보인다(다음 날 다시 잡힐 확률 자체가 올라가므로).

---

## 5. 문서 규율 — 이번에 새로 생긴 것

- `.claude/skills/reload-docs/SKILL.md` §1.8 — 컴팩트 후 Play Console 현황을 크롬으로 갱신(조건부, 읽기 전용)
- `C:\project\common\PLAY_CONSOLE_STATUS.md` — 스토어 실황 정본.
  ⚠ **§2-1(콘솔)과 §2-4(OTA)를 함께** 봐야 한다 — 콘솔만 보면 "사용자가 실제로 도는 코드"를 모른다.
