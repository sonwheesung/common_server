# 다음 작업 (2026-09-07 갱신)

**미결 목록**이다. 끝나면 지우거나 PLAN으로 옮긴다 — 남겨두면 썩는다.
설계 배경은 `docs/PLAN.md` Phase 12·13, 세션 간 협업은 `docs/MULTI_SESSION.md`,
스토어 현황은 `C:\project\common\PLAY_CONSOLE_STATUS.md`(콘솔 실측 정본).

---

## ✅ 끝난 것 — 2026-09-02 (설계는 PLAN Phase 13으로 옮김)

- **웜 스타트 하트비트** `POST /v1/heartbeat` + SDK `heartbeat()` — 배포 완료(`f418c0f`)
- **`exp` 클레임 이관** — 만료 기준이 서버 한 곳으로. `SESSION_TTL_DAYS`는 레거시 폴백으로 강등
- **SDK `2026-09-02`** — 네 세션에 스펙 발송 완료(재복사 + `AppState` 리스너)

검증: 프로덕션 가드 `_dv_public 14` · `_dv_auth 27` · `_dv_sdk 41` — 전부 PASS.
성공 경로는 `tools/_e2e_heartbeat.ts` 10/10(로컬·프로덕션 각 1회, 테스트 행은 정리).

---

## 🔴 1. 문의 알림이 "누가 보냈나"를 못 가른다 — 60시간 오탐이 실제로 났다

**사건**(2026-09-07 확정, diary 세션 + 사용자 확인): 9/4 21:49 조각 문의 1건은 **실사용자가 아니라
우리가 실기기 검증에서 보낸 것**이다(로컬 빌드 `0.2.5` · 트랙에 간 적 없음 · 게시 23:43보다 2시간 빠름).
`tickets_stale` 이 60시간 울렸고, 나는 그걸 "첫 실사용자 버그 리포트"로 사용자에게 보고했다.

🔴 **사고가 아니라 구멍이다. 그리고 다음번 비용은 반대 방향이다** — 오탐에 익숙해지면 **진짜를 무시한다.**

### 1-1. 표식이 하나뿐이다 (diary 세션 지적)

가드가 만든 문의는 본문 접두사 `[_dv_public]` 로 걸러지는데, **사람이 손으로 보낸 검증에는 표식이 없다.**
서버는 그 둘을 구분할 방법이 아예 없다.

- 조각 쪽 규약은 문서화됐다(`C:\project\diary` `docs/SUPPORT_SYSTEM.md` §5.6, 커밋 `7d8afe3`):
  검증 문의는 ① 본문 첫머리에 `[테스트]` ② **같은 세션에서 닫는다**.
- 🚫 앱에 별도 "테스트 문의" 경로는 **안 만든다** — 검증의 값이 **실제 경로를 그대로 지나는 데** 있다
  (그게 9/4 로그인 복구를 확인한 방법이다). 우회로를 만들면 검증이 검증을 안 하게 된다.
- ⏭ **서버가 할 수 있는 것**: 접두사 규칙을 `[_dv_public]` **하나가 아니라 표식 목록**으로 둔다.
  그러면 형제 앱의 손검증(`[테스트]`)도 같이 걸러진다.

### 1-2. 🔴 그런데 더 큰 게 있다 — **대시보드와 문의 목록이 다른 것을 세고 있다** (실측 확인)

`CLAUDE.md` 규약: *"문의 미처리 = 대기(open) + 확인 중(reviewing). **뱃지·대시보드·기본 필터가 같은 정의를 쓴다**."*
**지금 안 그렇다.**

| | 가드 문의(`[_dv_public]`)를 | 근거 |
|---|---|---|
| `GET /api/admin/tickets` (목록) | **뺀다** (`notDev`, 기본 hide) | `app/api/admin/tickets/route.ts:30` |
| `GET /api/admin/stats` (KPI · `tickets_stale`) | **센다** — 필터가 아예 없다 | `app/api/admin/stats/route.ts:93~104` |

→ 가드 문의가 `open` 으로 하나 남으면 **알림은 영원히 울리는데 콘솔 목록에는 그게 안 보인다.**
   운영자는 "울리는데 뭐가 문제인지 모르는" 화면을 보게 된다.
⚠ **지금 당장 터져 있지는 않다** — 4앱 모두 `ticketsPending` 이 조각 1건 말고 0이다(9/7 실측).
   구조가 그렇다는 것이지 현재 오작동 중은 아니다.

### 1-3. 남은 결정 (사람)

- [ ] **조각 문의 1건을 닫을 것인가** — 우리 검증으로 확정됐으니 닫으면 `tickets_stale` 이 그친다.
      운영 DB write라 **사람 승인 후에** 한다. diary 세션은 `ADMIN_TOKEN` 규약상 콘솔을 열지 않는다.
- [ ] **1-1 + 1-2 를 고칠 것인가** — 한 군데(`stats` 의 문의 3개 질의)에 목록과 같은 필터를 걸고,
      그 필터를 `[_dv_public]` · `[테스트]` **목록**으로 둔다. 계약이 바뀌므로 `_dv_admin` 가드도 같이 늘린다.

### 1-4. 🔴 가드가 이걸 못 잡은 이유 — **두 곳을 대조하는 검사가 0이다**

`_dv_admin` 은 70개를 돌리는데, 그 안에 이런 짝이 있다:

```
tools/_dv_admin.ts:183   "tickets 기본 조회에 가드 문의가 안 섞인다"   ← 목록만 본다
tools/_dv_admin.ts:100   "stats 200 + 형태"                        ← 모양만 본다
```

**둘 다 통과한다. 둘이 같은 것을 세는지는 아무도 안 본다.**
`CLAUDE.md` 의 🔴 *"돌면서 아무것도 안 보는 검사"* 항목과 같은 계열인데 형태가 하나 더 있다 —
검사가 **허위**여서가 아니라 **각자 자기 화면만 봐서** 생긴 구멍이다. 개수 축(`MIN_CHECKS`)으로도,
허위 오라클 스캐너로도 안 잡힌다.

→ 고칠 때 **필터만 넣고 끝내지 말 것.** 같이 넣을 검사는 이것이다:
```
같은 앱에 대해 stats.kpi.ticketsPending  ==  tickets?status=open|reviewing 의 건수
(가드 문의를 하나 만들어 두고 돌리면 지금 코드에서는 FAIL 해야 한다 — 그게 이 검사의 변이 테스트다)
```
★ 이 형태를 조각 세션이 **자기 서버에서 독립적으로 발견했다**(2026-09-07 · `ai_usage` 가 재생성 원가를
덮어써서 콘솔과 라우트가 같은 사건을 다르게 셈 · `AI_REPORT_SYSTEM §6.6.1`). 그쪽도 검사는 41개인데
**두 곳을 대조하는 검사가 0**이었다. **한 프로젝트의 버그가 아니라 우리 검사 습관의 구멍이다.**

**빠진 것에 이름을 붙이면 이렇다**(조각 세션이 자기 가드 18개를 전수해 분류한 결과 · 2026-09-07):

| 대조의 종류 | 조각 | 우리 |
|---|---|---|
| **왕복** — 한 계산과 그 역함수 | 있다(암복호화 KAT · i18n 라운드트립) | 있다(`_dv_contrast` 의 오라클 5개) |
| **환경 간** — 같은 계산을 지역·언어별로 | 있다(시간대 7 · 언어 15) | 부분적 |
| 🔴 **경로 간** — *서로 다른 두 코드 경로가 같은 사실에 같은 답을 내는가* | **0** | **0** |

앞의 둘은 **한 계산을 여러 입력으로** 돌린다. 빠진 셋째는 **두 계산을 한 입력으로** 돌린다 —
`stats` 와 `tickets` 처럼 **서로를 모르는 두 코드가 같은 사실을 각자 답할 때** 그 답이 같은지 보는 것이다.
우리가 오늘 찾은 결함도, 조각의 원가 누락도 정확히 그 칸에 있었다.

🔴 **그리고 대조하는 쪽도 대조당해야 한다**(조각 세션 실측). 그쪽 `check:timezone` 은 2026-09-02에
**지역 수를 단언 수로 세고 있어서 단언을 전부 지워도 초록불**이었다 — "환경 간 대조"조차 자기 자신은 못 본다.
→ 경로 간 검사를 새로 만들 때 **그 검사의 개수 보고도 함께 대조한다.** `CLAUDE.md` 의 `MIN_CHECKS` 규율이
   바로 그 자리인데, 새 검사가 자기 개수를 부풀리면 그 바닥도 같이 속는다.

⚠ **조각은 원격 관측이 0이라 이 알림을 자기가 받지 못한다** — 이 서버의 알림이 **조각의 유일한 외부 신호**다
(9/4 구글 로그인 사고도 "사람이 실기기를 만져본 것"이 유일한 발견 경로였다). 오탐을 방치하면 그 하나가 죽는다.

---

## ✅ 2. 앱 회신 — 넷 다 코드가 붙었고 **넷 다 신호가 왔다**

**확인 시각 2026-09-07 10:39 KST** (서버 신호 4앱 동시 + Play Console 실측).

| 앱 | 스토어 | 서버 웜 신호 | 판정 |
|---|---|---|---|
| `myword` | 프로덕션 19/1.3.3 · 145국 | ✅ 2026-09-02 | 🟢 도달 |
| `linkmemo` | 프로덕션 11/1.0.7 · 177국 | ✅ 2026-09-02 | 🟢 도달 |
| `idearepository` | 프로덕션 11/1.0.10 · 177국 | ✅ 2026-09-02 | 🟢 도달 |
| `jogak` | 🆕 **프로덕션 19/0.2.6** (9/4 **23:43** 게시 · 1개국) | ✅ **2026-09-04** | 🟢 계측 확인 |

🟢 **조각 건은 기다린 것이 맞았다.** 9/3에 `warmSince: null` 이던 것을 "미부착"으로 굳혔으면 오진이었다.
그 앱 DAU가 9/3에 **0**이라 웜 이벤트가 **원리적으로** 0이었을 뿐이고, **9/4에 주체 4개가 들어오자
같은 날 신호가 왔다.** `CLAUDE.md` 에 박아 둔 ①/② 규칙이 실제 사건에서 ②로 판정된 첫 사례다.

⚠ **그 4개를 "프로덕션 사용자"로 읽으면 안 된다**(9/7 정정). 생성 시각이 17:29 · 18:15 · 21:37 · 22:48 KST 라
**전부 게시(23:43) 이전**이다 — 알파 테스터 아니면 개발자 기기다. **계측이 도는 증명이지 도달의 증명이 아니다.**

🔴 **조각이 9/4에 프로덕션으로 나간 것을 이 저장소는 사흘 동안 몰랐다.** 콘솔에서 일어난 일은
어느 세션에도 전달되지 않는다 — `PLAY_CONSOLE_STATUS.md` §0-2 가 말하는 바로 그 상황이다.
✅ 문의의 `0.2.5` 는 **두 번 올린 게 아니다** — 트랙에 간 적 없는 로컬 검증 빌드다(diary 세션 회신).
조각 결번은 이제 셋이다: `vc10`(같은 날 11로 덮음) · `vc14`(semver 끊기용) · `vc18`(실기기 검증 전용).

---

## 🔴 3. 정정 — "idearepository 자연 도달 확정"은 **취소됐다. 스크립트였다**

9/3에 이 문서와 `PLAY_CONSOLE_STATUS.md` §5-1 에 *"이 계정의 첫 자연 도달 표본"* 이라고 적었다.
**틀렸다.** 2026-09-07 운영 DB 실측:

| | 실측 |
|---|---|
| 주체 15개 생성 | 9/2 **10:42:48 ~ 10:46:32** — 약 **15초 등간격** |
| 그 15개 마지막 접속 | 9/4 **09:43:32 ~ 09:47:13** — **같은 순서 · 같은 15초 간격** |
| 활성 일자 | 15개 **완전 동일**: 9/2 · 9/3 · 9/4 |
| 시각 비트 | 15개 **완전 동일**: 9/2 10시 · 9/3 10시 · 9/4 9시 |

대조군이 분명하다 — 같은 질의로 묶으면 **조각·링크메모·마이워드는 전부 제각각**이고 이 모양은
idearepository 에서만 나온다. 빼고 나면 **9/3·9/4 DAU 16 은 전원 스크립트**이고, 9/5 이후로는
사람 신호가 0이다(9/6의 1건은 8/18부터 있던 개발자 기기).

→ **이 계정에 확정된 자연 도달 표본은 아직 없다.**
→ 그 스크립트의 정체는 **이 저장소 밖**이다. `tools/*` 는 기기를 1개 만들고 지운다(`_e2e_heartbeat.ts`).
   **idearepository 세션에 물어야 확정된다** — 미결.

★ **재보정 (`PLAY_CONSOLE_STATUS.md` §5-1 과 같은 내용):**
```
DAU 가 크다는 것은 `DAU ≥ 2` 오라클을 더 강하게 통과시키지 않는다.
규칙성(생성 간격 · 활성일 · 시각 비트가 서로 같은가)을 먼저 본다.
24 는 2 보다 안전하지 않다 — 자동화라면 24 쪽이 훨씬 쉽다.
날짜를 빼는 것으로는 못 막는다. 이 15개는 생성 다음날·다다음날에도 찍었다.
오라클의 축은 날짜가 아니라 **주체**여야 한다.
```

---

## ⚠ 4. 네 앱 모두 사람 접속이 멈췄다 (관측 사실 · 조치 미정)

| 앱 | 마지막 사람 접속 (KST) |
|---|---|
| `idearepository` | 2026-09-06 23:29 (개발자 기기 1) |
| `jogak` | 2026-09-04 23:43 |
| `linkmemo` | 2026-09-04 11:22 |
| `myword` | 2026-09-03 10:27 |

누적 주체는 링크메모 40 · idearepository 36 · 조각 11 · 마이워드 8 인데 **최근 7일 활성(WAU)은
15 · 30(스크립트 포함) · 6 · 8** 이다. 서버 문제는 아니다 — `last_seen_at` 과 `subject_active_day`
가 **같은 시점에서 함께** 끊겼다(둘 중 하나만 끊기면 기록 경로 고장이다).

---

## ✅ 5. 색 대비 — **내가 어제 틀렸다**(2026-09-02 정정, 커밋 아래)

~~다크 테마 셋이 AA 미달: 보조 텍스트 3.26 · accent 배지 3.60 · ok 배지 4.31~~
→ **전부 틀린 숫자였다.** 다시 계산하니 각각 **7.25 · 5.20 · 6.30 으로 원래부터 통과**였다.
그 "처방"대로 고쳤으면 멀쩡한 색을 셋 더 흔들 뻔했다.

🔴 **그리고 어제 실제로 한 수정(커밋 `6ff4d9b`)이 반대였다.**
"L70 파랑 위 근검정이 3.70:1 미달"이라며 근백으로 바꿨는데, 실제 값은:

```
근검정  6.66:1  통과   ← 원래 상태
근백    2.65:1  미달   ← 내가 바꾼 상태
```

밝은 면 위에서는 **어두운 글씨의 대비가 크다.** 착시가 아니라 산수인데 반대로 적었고,
그 숫자가 커밋 메시지·CSS 주석·이 문서 세 곳에 그대로 퍼졌다.
⚠ 사용자가 "파란 버튼에 검은 글씨가 이상하다"고 한 것은 **맞았다** — 관례상 어색한 게 맞다.
틀린 건 내가 거기에 **가짜 접근성 근거를 붙인 것**이다.

**고친 방법**: 글자를 뒤집는 게 아니라 **면을 나눴다**. `--color-accent` 하나가 세 역할
(차트 막대 / 배지 글자 / 버튼 배경)을 겸해 한 값으로 셋을 만족시킬 수 없었다.
글자를 얹는 면만 `--color-accent-strong`(다크 L54)으로 분리 — 흰 글씨 5.04:1.
덤으로 **원래부터 미달이던 라이트 warn 배지**(3.95:1)도 찾아 고쳤다(L58 → L52, 5.07:1).

**재발 방지**: `tools/_dv_contrast.ts` 신설. `globals.css`를 **읽어서** 27개 조합을 계산하고
미달이면 FAIL 한다. 오라클 5개가 계산기 자체를 먼저 검증한다(그중 하나가 "밝은 파랑 위에선
검은 글씨가 흰 글씨보다 대비가 크다" — 어제 틀린 그 명제다).
→ **이제 대비는 주장할 수 없고 계산된다.**

---

## 6. 관측: 저절로 켜지는 것들

손댈 필요 없이 시간이 해결한다. **안 켜지면 그때 의심한다.**

- ~~시간대별 활성~~ → ✅ **네 앱 전부 켜졌다**(2026-09-07 실측 `hourChartReady: true` · 경과일 6~7).
  ⚠ **앱마다 값이 다르다** — 9/3에는 `myword 3/true` 옆에서 `idearepository`가 `2/false`였다.
  게이트는 서버 전체가 아니라 **그 앱의 수집 경과일**을 본다.
- 요일 평균 — 수집 14일차에 켜진다(2026-09-07 현재 `coverageDays: 6~7` · `chartReady: false` — 정상. 9/14 무렵)
- 잔디 — 날짜가 쌓일수록 채워진다. 2026-09-01 이전은 **점선(수집 전)** 으로 남는 게 정상이다

🔴 ⚠ **웜 스타트 계측이 붙기 전과 후는 같은 축이 아니다.** 붙은 날 그 앱의 DAU가 뛰면
그건 사용자가 는 게 아니라 **계측이 는 것**이다. 화면이 그걸 말하지 않으면 다음 사람이
"9월 초에 성장했다"로 읽는다 — Phase 12가 막아둔 거짓말 셋과 같은 종류다.
→ 앱별 **계측 개시일**을 콘솔이 알고 있어야 한다(지금은 `coverageDays`가 서버 전체 축이다).

---

## 7. R8 미적용 — 조각·My Word 같은 축 (미착수 · 서버 밖)

조각 프로덕션 트랙에 정책 경고 1건: *"앱 최적화가 기준점 미만 · 난독화 2%"* · 해결 기한 **2027-02**.
🔴 **당장의 대가는 경고가 아니라 관측이다** — R8 미적용이라 `mapping.txt` 가 없고, 그래서 **비정상 종료·ANR 을
지금 읽을 수 없다**(diary 세션 지적). My Word 의 R8 건(`PLAY_CONSOLE_STATUS.md` §2-4)과 같은 축이라 묶어 볼 것.
⚠ 서버 작업이 아니다 — 앱 빌드 설정이다. 여기엔 **잊지 않으려고** 적어 둔다.

---

## 8. 리텐션 D1/D3/D7 (미착수)

`subject_active_day`가 있으므로 **근사가 아니라 진짜 코호트**를 만들 수 있다(배구는 `lastSeenAt` 근사).
다만 D7은 7일치가 쌓여야 말할 수 있고, **위 6번이 먼저다** — 계측이 바뀐 경계를 걸치는 코호트는
리텐션이 좋아진 것처럼 보인다(다음 날 다시 잡힐 확률 자체가 올라가므로).

---

## 9. 문서 규율

- `.claude/skills/reload-docs/SKILL.md` §1.8 — 컴팩트 후 Play Console 현황을 크롬으로 갱신(조건부, 읽기 전용)
- `C:\project\common\PLAY_CONSOLE_STATUS.md` — 스토어 실황 정본.
  ⚠ **§2-1(콘솔)과 §2-4(OTA)를 함께** 봐야 한다 — 콘솔만 보면 "사용자가 실제로 도는 코드"를 모른다.
