node_modules/
.next/
.vercel/
out/
build/

# env — 실제 값은 절대 커밋 금지(.env.example만 커밋)
.env
.env.local
.env*.local
.env.local.bak-*

*.tsbuildinfo
next-env.d.ts

.DS_Store
*.log

.vercel
.env*
# ↑ Vercel CLI가 추가한 광범위 규칙. .env.example 은 시크릿이 없는 템플릿이라 커밋해야 한다.
!.env.example

# Claude Code 로컬 설정(개인 훅·권한) — 스킬(.claude/skills/)은 커밋한다
.claude/settings.local.json
